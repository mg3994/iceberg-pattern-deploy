name: tasks
description: "Self-contained feature package for Task Management implementing the Iceberg Pattern."
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.14.0-95.2.beta

dependencies:
  flutter:
    sdk: flutter
  signals_core: ^7.0.0
  bloc_signals_flutter: ^1.3.1
  fast_immutable_collections: ^11.2.0
  drift: ^2.35.0
  core:
    path: ../../core
